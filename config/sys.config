[
  {epoch, [
      {time_delta_to_fetch_new_txs, 30000} %% in milliseconds
  ]},
  {lager, [
      {handlers, [
          {lager_file_backend,
            [{file, "log/epoch.log"}, {level, info}, {size, 41943040}, {date, "$D0"}, {count, 10}]}
      ]}
  ]}
].
